Multiple servers connected together to work a single system 
If one server fail then other server will take care of it and all of them are sharing workload with each other.

Eg
           Users
             │
             ▼
      Load Balancer
        / \
       ▼ ▼
 Linux Server1 Linux Server2
       │ │
       └────┬─────┘
            │
         Shared Storage
Advantage 
High availability 
If one server want to do calculation then 100 server will do work together 

Trend micro is a cyber security software which install on computer, server, cloud and network to identify malware and cyber security and protect our system 
from them.
If any malicious file is downloaded or if any suspicious activity occur then it will scan the file, and if that file is virus or malware then 
trend micro will mark taht file as dangerous

Sometimes it out file in other location so that it won't harm to other files.