find / --perm /u=s

find = seacrh file
exec = perform operation on that file

seacrh files and give detailed list
find /home/bilal -name "employee.txt" -exec ls -l {} \;

delete all .log files
find /var/log -name "*.log" -exec rm {} \;

change permission
find . -name "*.sh" -exec chmod +x {} \;

give the content of a file
find . -name "README.md" -exec cat {} \;

{} = whatever input you there, place input here

\; = the command finishes