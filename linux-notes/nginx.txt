it works like apache

Browser
    ↓
Nginx
    ↓
Website Files
    ↓
Browser

both are serving website

consider java application running on port 8080. but browser didn't know about this, hence we use nginx

nginx sends request to java app, java app send response to nginx

Browser
    ↓
Nginx (Port 80)
    ↓
Java Application (Port 8080)

package name - nginx

Load Balancer:
if application has 3 copies and 1000 users came and login into 1st copies and that 1st copie get crashed. to avoid this, nginx divide request into multiple copies

Reverse Proxy:
in ffice there is a CEO (website). noone wants to directy communicate with ceppo, first they need to communciate to receptionalist (application),