storage area network
it is a storgae folder where it only share disk instead of files, folder in a network
server will got assume that new hard disk (/dev/sdb) is attached into it
Server1 ----\
             \
              SAN Switch ---- Storage Array
             /
Server2 ----/
After that adminstrator make partition and filesystem in it.

make filesystem
sudo mkfs.ext4 /dev/sdb

mount data
sudo mount /dev.sdb 
/data
best sharing for database, vmware

protocols: iSCSI, fibre channel (FC)