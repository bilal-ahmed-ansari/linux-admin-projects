If it is applied on file, then if any user will execute that file then file will execute by that group
If we apply on directory, then if any user create a file that file will assign same group permission as of parent directory 
There is one folder where your team members can access those folders 

Nick user login
Consider he is a member of qa group
Create a directory inside tmp
Give all permission to that directory so that other user can create, edit and delete the file

Login with paul user but he is not part of qa
Paul go to that directory and create a file
If nick wants to edit that same file, he won't have a permission 
Now nick set sgid on that directory 

Even root user create a file with root login, that file will have a group in permission called qa